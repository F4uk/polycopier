# Build output
/target

# Environment files — never commit secrets
.env
.env.local
.env.production

# Per-environment tunable config — auto-generated from config.example.toml on first run
# Commit config.example.toml instead; each environment manages its own config.toml
config.toml

# Frontend build output
web/dist/

# Runtime logs and data
*.log
bot.log

# Persistent runtime state — auto-created on first trade, never commit
copy_ledger.json
sim_copy_ledger.json
*.json.tmp

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Windows build tool installers — not part of the project
rustup-init.exe
vs_BuildTools.exe

# Rust
.agents/
